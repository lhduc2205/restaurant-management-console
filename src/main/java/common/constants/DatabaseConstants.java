package common.constants;

public class DatabaseConstants {
    // JSON
    public static String MENU_JSON = AppPath.JSON_DATABASE + "/menu.json";
    public static String MENU_ITEM_JSON = AppPath.JSON_DATABASE + "/menu_item.json";

    // CSV
    public static String MENU_CSV = AppPath.CSV_DATABASE + "/menu.csv";
    public static String MENU_ITEM_CSV = AppPath.CSV_DATABASE + "/menu_item.csv";
}
